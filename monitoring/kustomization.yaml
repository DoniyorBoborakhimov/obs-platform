apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - prometheus/rules/payment-api-alerts.yaml
  - prometheus/rules/payment-api-slo.yaml
  - prometheus/rules/payment-api-diagnostics.yaml
  - prometheus/servicemonitor-payment-api.yaml
  - grafana/dashboard-payment-api-red.yaml
  - grafana/dashboard-payment-api-slo.yaml
  - alertmanager/webhook-logger.yaml

labels:
  - pairs:
      managed-by: argocd
    includeSelectors: false
